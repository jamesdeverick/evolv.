# Core dependencies
streamlit>=1.28.0
pandas>=2.0.0
requests>=2.31.0
python-dotenv>=1.0.0

# Web scraping
beautifulsoup4>=4.12.0

# LLM integration
litellm>=1.17.0
anthropic>=0.39.0

# File processing
PyPDF2>=3.0.0
defusedxml>=0.7.1

# Visualization (optional but recommended)
plotly>=5.14.0

# NLP & Semantic Analysis for Generative Search Optimization
spacy>=3.7.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0

# Note: After installing requirements, download spaCy model with:
# python -m spacy download en_core_web_sm

# Note: blinker is a dependency of streamlit but may conflict with system version
# If install fails with blinker error, use: pip install -r requirements.txt --ignore-installed blinker

# Note: For cloud deployment (HuggingFace Spaces):
# - Set environment variable LLM_PROVIDER=deepseek
# - Configure DEEPSEEK_API_KEY in Secrets
# - No local Ollama installation required
